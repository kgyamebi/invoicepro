# Billing

Plans and prices live in `plans` and `plan_prices`. Seed data is an example only. Admins can change amounts and document limits.

Free users get a monthly document allowance. When the allowance is used up they can wait, upgrade, or spend credits.

Unlimited plans still have a fair-use cap (`fairUseCap`) so a single tenant cannot exhaust shared infrastructure.
